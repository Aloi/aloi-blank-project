To get this example working:
1. Set your document root in Apache to the WebRoot folder. 
2. Ensure that the directory "app/cache" is writable by the web user (chmod 777 app/cache)

For more information go to http://aloi-project.org